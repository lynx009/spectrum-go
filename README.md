# spectrum-go

> **spectrum-go** 是一个用 Go 语言实现的智谱 AI（ZhipuAI）第三方 SDK。本项目旨在通过复刻官方逻辑，深入学习 Go 语言在处理 **JWT 动态鉴权**、**SSE 流式响应**、以及**多模态异步任务轮询**等复杂场景下的工程实践。

[![Go Version](https://img.shields.io/badge/Go-1.21%2B-blue)](https://golang.org/)
[![License](https://img.shields.io/badge/License-MIT-green)](LICENSE)

> **Disclaimer:** 本项目仅供学习交流使用，非智谱 AI 官方发布。

---

## 🚀 项目亮点 (Features)

> 以下为本项目的目标特性，将随路线图各阶段逐步实现。

| 特性 | 描述 |
|------|------|
| **全模态覆盖** | 计划支持 ChatGLM（文本）、CodeGeeX（代码）、CogView（图像）及 CogVideoX（视频）等全系列模型 |
| **原生并发安全** | 充分利用 Go 的 `Context` 和 `Channel` 机制，支持高并发下的流式对话（SSE） |
| **智能任务轮询** | 针对视频与图像生成，内置自动退避算法的异步任务状态追踪器 |
| **零依赖鉴权** | 纯 Go 实现智谱特有的 API Key 签名与 JWT 管理，无需额外部署 |
| **工程化设计** | 严格遵循 Go 模块化设计，接口清晰，易于扩展 |

---

## 🏗️ 架构预览 (Architecture)

项目的核心设计采用了**分级分包策略**，确保代码的可读性与维护性：

```
spectrum-go/
├── client/         # 统一的入口，管理 HTTP 周期与全局配置
├── auth/           # 负责 API Key 的动态令牌签发（JWT）
└── provider/
    ├── chat/       # 文本对话与流式处理（ChatGLM）
    ├── vision/     # 多模态理解与图像生成（CogView）
    └── media/      # 视频生成任务（CogVideoX 异步工作流）
```

---

## 🛠️ 快速开始 (Quick Start)

### 安装

```bash
go get github.com/lynx009/spectrum-go
```

### 初始化客户端

```go
package main

import (
    "fmt"
    "github.com/lynx009/spectrum-go/client"
)

func main() {
    // 使用您的智谱 AI API Key 初始化客户端
    c := client.New("your-api-key")

    // 发起一次文本对话（同步模式）
    resp, err := c.Chat().Completions("ChatGLM-4", "你好，请介绍一下你自己。")
    if err != nil {
        panic(err)
    }
    fmt.Println(resp)
}
```

### 流式对话（SSE）

```go
stream, err := c.Chat().Stream("ChatGLM-4", "用 Go 写一个快速排序。")
if err != nil {
    panic(err)
}
for chunk := range stream {
    fmt.Print(chunk.Content)
}
```

---

## 🎯 学习路线图 (Roadmap)

- [ ] **Phase 1**: 实现基于 JWT 的核心鉴权逻辑 (Auth Center)
- [ ] **Phase 2**: 封装 ChatGLM-4 文本生成（同步 / 流式）
- [ ] **Phase 3**: 攻克 CogVideoX 异步任务轮询机制
- [ ] **Phase 4**: 完善错误处理与 Rate Limiting 策略

---

## 📄 许可证 (License)

本项目采用 [MIT License](LICENSE) 开源协议。
